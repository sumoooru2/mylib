//joke :)

//use clang++

#include"rprint.hpp"

#define もし if(
#define ： )
#define ならば )
#define と ,
#define が ==
#define かける *
#define 足す +
#define 引く -
#define とする ;
#define から ;
#define である ;
#define で有る限り最後に ;
#define を =
#define 小なり <
#define 小なりイコール <=
#define を１増やす ++
#define 以下を繰り返すこと for(
#define を引数 (
#define で呼ぶ );
#define で呼んだもの )
#define として )
#define 結果は return
#define を返す /**/
#define 無し /**/
#define は (
#define を引数に取り ){
#define 引数を取らず ){
#define というもの }
#define プログラムの処理は int main(){

#define 整数 int
#define 整数の int
#define 何も返さない void

template <class... A>
auto 出力(A&&... a){
    prints(std::forward<A>(a)...);
}

整数　を返す　足し算　は　整数の　入力　と　整数の　入力２　を引数に取り
    結果は　入力　足す　入力２　である
というもの

整数 を返す 階乗　は　整数の　入力　を引数に取り
    もし　入力　が　1　ならば
        結果は 1 である
    結果は 入力　かける　階乗　を引数　入力　引く　1　で呼んだもの　である
というもの

整数　を返す　階乗２ は　整数の　入力 を引数に取り
    整数の　答え を 1　とする
    以下を繰り返すこと　整数の　い を 1　から　い　小なりイコール　入力　で有る限り最後に　い　を１増やす として
        答え を 答え　かける　い　とする
    結果は　答え　である
というもの

何も返さない テスト は　引数を取らず
    整数の　あ を 1　とする
    出力　を引数　あ　で呼ぶ
    出力　を引数　1 と　3 で呼ぶ
    出力　を引数　足し算　を引数　1 と　2 で呼んだもの　で呼ぶ
    出力　を引数　階乗 を引数　10　で呼んだもの　で呼ぶ
    出力　を引数　階乗２　を引数　10　で呼んだもの　で呼ぶ
というもの

// プログラムの処理は
//     テスト　を引数　無し　で呼ぶ
// というもの
